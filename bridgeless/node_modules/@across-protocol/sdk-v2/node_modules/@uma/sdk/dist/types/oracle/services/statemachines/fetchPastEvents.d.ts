import Store from "../../store";
import { Handlers as GenericHandlers } from "../../types/statemachine";
import { RangeState } from "../../utils";
export declare type Params = {
    chainId: number;
    startBlock?: number;
    endBlock?: number;
    maxRange?: number;
};
export declare type Memory = {
    error?: Error;
    state?: RangeState;
    iterations: number;
};
export declare function initMemory(): Memory;
export declare function Handlers(store: Store): GenericHandlers<Params, Memory>;

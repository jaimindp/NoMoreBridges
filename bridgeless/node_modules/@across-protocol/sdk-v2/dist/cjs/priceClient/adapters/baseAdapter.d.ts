export type BaseHTTPAdapterArgs = {
    timeout?: number;
    retries?: number;
};
export declare class BaseHTTPAdapter {
    readonly name: string;
    readonly host: string;
    private _retries;
    private _timeout;
    get retries(): number;
    set retries(retries: number);
    get timeout(): number;
    set timeout(timeout: number);
    constructor(name: string, host: string, { timeout, retries }: BaseHTTPAdapterArgs);
    protected query(path: string, urlArgs?: object): Promise<unknown>;
    protected sleep(ms: number): Promise<void>;
}

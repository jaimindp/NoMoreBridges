import { ChainContext } from "../..";
export function chainFactory(p, chain) {
    return p.getChain(chain);
}
export class MockChain extends ChainContext {
    constructor(chain, platform) {
        super(chain, platform);
    }
}
//# sourceMappingURL=chain.js.map
import { CosmosGovV1Beta1Tx, } from '@injectivelabs/core-proto-ts';
import { MsgBase } from '../../MsgBase';
import snakecaseKeys from 'snakecase-keys';
/**
 * @category Messages
 */
export default class MsgVote extends MsgBase {
    static fromJSON(params) {
        return new MsgVote(params);
    }
    toProto() {
        const { params } = this;
        const message = CosmosGovV1Beta1Tx.MsgVote.create();
        message.option = params.vote;
        message.proposalId = params.proposalId.toString();
        message.voter = params.voter;
        return message;
    }
    toData() {
        const proto = this.toProto();
        return {
            '@type': '/cosmos.gov.v1beta1.MsgVote',
            ...proto,
        };
    }
    toAmino() {
        const proto = this.toProto();
        const message = {
            ...snakecaseKeys(proto),
        };
        return {
            type: 'cosmos-sdk/MsgVote',
            value: message,
        };
    }
    toWeb3() {
        const amino = this.toAmino();
        const { value } = amino;
        return {
            '@type': '/cosmos.gov.v1beta1.MsgVote',
            ...value,
        };
    }
    toDirectSign() {
        const proto = this.toProto();
        return {
            type: '/cosmos.gov.v1beta1.MsgVote',
            message: proto,
        };
    }
    toBinary() {
        return CosmosGovV1Beta1Tx.MsgVote.encode(this.toProto()).finish();
    }
}
//# sourceMappingURL=MsgVote.js.map